let btnEnv = document.getElementById("btnEnviar");
let btnEnv = document.getElementById("btnEnviar2");
let base = document.forms[0].base;
let altura = document.forms[0].altura;
let rotulo = document.getElementById("rotulo");

btnEnv.addEventListener("click", () => {
  // TU CODIGO VA AQUI, POR EJEMPLO
  console.log("El area es", (base * altura);
});
rotulo.innerHTML = "Indique la base y la altura ";
